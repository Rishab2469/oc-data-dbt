version: 2

models:
  - name: staging_ny_model
    description: "Staging layer model for NY jurisdiction built from raw_ny_model."
    columns:
      - name: id
        description: "The unique identifier."
        tests:
          - unique
          - not_null
      - name: name
        description: "A sample name column."
