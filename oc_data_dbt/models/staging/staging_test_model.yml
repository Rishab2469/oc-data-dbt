version: 2

models:
  - name: staging_test_model
    description: "Staging layer model built from raw_test_model."
    columns:
      - name: id
        description: "The unique identifier."
        tests:
          - unique
          - not_null
      - name: name
        description: "A sample name column."
