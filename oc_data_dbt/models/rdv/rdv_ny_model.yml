version: 2

models:
  - name: rdv_ny_model
    description: "RDV layer model for NY jurisdiction built from staging_ny_model."
    columns:
      - name: id
        description: "The unique identifier."
        tests:
          - unique
          - not_null
      - name: name
        description: "A sample name column."
