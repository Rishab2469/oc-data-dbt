version: 2

models:
  - name: rdv_test_model
    description: "RDV layer model built from staging_test_model."
    columns:
      - name: id
        description: "The unique identifier."
        tests:
          - unique
          - not_null
      - name: name
        description: "A sample name column."
