version: 2
models:
  - name: raw_ny_model
    description: "A raw layer model for NY jurisdiction built from ext_table_ny."
    columns:
      - name: id
        description: "The unique identifier."
        tests:
          - unique
          - not_null
      - name: name
        description: "A sample name column."
